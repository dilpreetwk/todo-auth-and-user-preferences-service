package org.example.todo.auth.and.user.preferences.service;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class TodoAuthAndUserPreferencesServiceApplication {

    public static void main(String[] args) {
        SpringApplication.run(TodoAuthAndUserPreferencesServiceApplication.class, args);
    }

}
